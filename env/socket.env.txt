# HRM Socket Service - Environment Configuration
# Instructions: Copy this file to config/generated/PRO/socket-service/ and rename to .env

SERVER_PORT=5001

# Auth API endpoint (inside Docker network)
AUTH_API=http://api-gateway:8080/api/auth/me

# RabbitMQ Configuration (Docker container)
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=hrm_user
RABBITMQ_PASSWORD=hrm_pass
RABBITMQ_WORK_QUEUE_NAME=hrm_socket_work_queue

# Node Environment
NODE_ENV=production
